---
const { tags } = Astro.props
---

<div class="tags">
    {
        tags.map((tag) => (
            <p class="tag">
                <a class="tagLink" href={`/tags/${tag}`}>{tag}</a>
            </p>
        ))
    }
</div>